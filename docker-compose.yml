services:
  postgres:
    image: postgres:17
    ports: ["5432:5432"]
    environment:
      POSTGRES_DB: pixel_agents
      POSTGRES_USER: pixel
      POSTGRES_PASSWORD: pixel
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
